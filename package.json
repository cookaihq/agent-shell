{
  "name": "agent-shell",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "postinstall": "node scripts/rebuild-better-sqlite3-electron.mjs",
    "test": "node scripts/vitest-electron.mjs run",
    "typecheck": "tsc -b --pretty",
    "publish:mirror": "node scripts/publish-mirror.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
