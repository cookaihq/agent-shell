# 更新日志（Changelog）

本文件记录 Agent Shell 每个发布版本面向用户的变化。版本号与 GitHub Release 一致（tag = `v<version>`）。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，每版按 `新功能 / 改进 / 修复 / 已知问题` 分类，只记录用户能感知的变化。

## [0.4.0] - 2026-06-25

本版带来「项目记忆」——Claude 在每个项目里会自动记住跨会话的要点，并能在引擎间共享；同时把后台常驻、已发送会话的设置锁定、挂起的选择卡持久化等做扎实，修了一批切换会话与排队相关的体验问题。

### 新功能

- **项目记忆**：用 Claude 时，agent 会自动记住该项目里跨会话的关键信息（你是谁、偏好、项目约定等），下次自然带上；右侧新增「记忆」标签可随时查看这些记忆。
- **记忆跨引擎共享**：可在项目里开启共享，让 Claude 与 Codex 共用同一份项目记忆；切换共享时会提示并合并已有内容。
- **新建项目可选目录**：新建项目时可以指定项目目录（自动创建新文件夹或选已有文件夹），不再只能用默认位置。

### 改进

- **已发送会话锁定设置**：会话一旦发出消息，它当时用的模型、权限、沙箱、协作模式就会记在这个会话上；之后切回这个会话会保持原样，不被全局设置带偏。
- **后台常驻更省心**：去掉了「保持后台运行」开关，应用默认就在后台常驻、需要时自动恢复，退出应用时再统一停止，行为更可预期。
- **后台会话在等你时有提示**：当某个后台会话里 agent 正等你做选择，会话列表对应行的最前面会显示一个待办图标；被中断的选择卡也能在你回来后继续。
- **资源管理器更顺手**：文件区头部重新整理为三段式，文件右键新增「打开方式」子菜单，斜着滑向子菜单不再误关。
- **引擎内核升级**：随上游更新 Claude 运行内核，跟进最新能力与稳定性修复。

### 修复

- **同项目切会话不再串台**：在同一项目里切换会话时，输入框草稿、耗时显示、滚动位置、文件改动浮层都会正确归属各自会话，不再互相串。
- **Claude 排队/出队体验**：修复连发消息排队后，出队那条没有气泡、发送按钮卡住、上一条耗时显示归零等问题。
- **首页计划模式入口**：在首页选「计划模式」档位现在能正确生效。
- **续聊与重连**：修复恢复会话时重复冒出「继续」气泡，以及排队中切会话 / 断线重连后消息丢失的问题。

### 已知问题

- mac 安装包未签名，首次打开会被 Gatekeeper 拦截，需**右键 → 打开**绕过。

## [0.3.5] - 2026-06-24

本版让 Codex 引擎也能弹出选择卡（含计划模式与「其他」自由输入），项目系统提示不再往项目目录里写文件，并修了几处切换会话、选择卡相关的体验问题。

### 新功能

- **Codex 也能弹选择卡了**：用 Codex 引擎时，agent 现在也能让你做选择（类似 Claude 的提问选择卡），还新增了「计划模式」档位；选项里可以选「其他」并自己输入内容。

### 改进

- **项目系统提示不再往项目里写文件**：以前给项目设置系统提示会在项目根目录生成一个 `AGENTS.md` 文件，现在改为直接注入、不再往你的项目目录里写文件，保持项目目录干净。
- **底层引擎升级**：随上游更新了 Claude 与 Codex 的运行内核，跟进最新能力与稳定性修复。

### 修复

- **同项目切会话不再串草稿**：在同一个项目里切换会话时，输入框里没发出去的草稿不会再串到别的会话；切过去会正确落回该会话自己的草稿。
- **Codex 计划模式选择卡挂起**：修复 Codex 在计划模式下选择卡可能卡住不动的问题；长时间未提交的选择会自动清理。
- **选择卡过高遮挡**：agent 提问的选择卡收紧了高度，不会再占用过多屏幕。
- **分组里新建项目**：在侧栏分组中点「+」新建项目，不会再出现顶部不冒出页签、或没有自动切到新项目的问题。

### 已知问题

- mac 安装包未签名，首次打开会被 Gatekeeper 拦截，需**右键 → 打开**绕过。

## [0.3.4] - 2026-06-23

本版补上了新版 macOS 上看不见的菜单栏图标，并把提醒做成全会话覆盖、处理后自动消失，另外新增一个自动对齐项目配置文件的内置技能。

### 新功能

- **菜单栏图标回来了**：在较新的 macOS 上，屏幕右上角状态栏之前看不到 Agent Shell 的图标，现在能正常显示了。点开图标有快捷菜单，也能在这里设置「开机自动启动」。
- **内置技能「CLAUDE/AGENTS 对齐」**：项目根目录里如果只有 `CLAUDE.md` 或只有 `AGENTS.md`，agent 会自动帮你把缺的那份补齐、保持两份内容一致；两份内容分叉时也会提示你。

### 改进

- **提醒覆盖所有会话**：以前提醒只在你当前打开的那个会话里弹，现在后台其它会话到点的提醒也能正常通知你；提醒卡处理过之后会自动消失，不再堆在那里。
- **选择卡更清爽**：agent 让你做选择（AskUserQuestion）时，选项改成了 VS Code 风格的列表行；一次问多个问题时，卡片顶部可以快速切换不同问题。
- **首启引导更顺**：首次启动的引导流程把「稍后再说」改成了「跳过」，每步逐步前进，最后一步才结束。

### 修复

- **提醒不再重复 / 不消失**：断线重连或重新进入会话后，提醒不会再重复弹出，处理完也会正确消失。

## [0.3.3] - 2026-06-23

本版重做了对话里的 Markdown 渲染，并给飞书远程接管补上了「文件推送」闭环，另外修复了切换会话时右侧预览区闪动的问题。

### 新功能

- **飞书文件推送**：开启飞书远程接管后，被接管的会话每跑完一轮，agent 这一轮新建或改动的文件会自动整理成一张卡片推到对应的飞书群；点卡片上某个文件的「查看」按钮，几秒内该文件就以飞书原生形式回到群里（图片内联、视频可播、其它可下载）。也可以在群里直接用大白话让 agent 把某个文件发给你。文件推送默认开启，并按常见后缀自动过滤；相关设置（总开关、文件类型、最多列出条数、是否含修改）在「设置 · 远程桥接 · 文件推送」里，改完自动保存、下一轮生效。

### 改进

- **Markdown 渲染全面升级**：对话里 agent 输出的 Markdown 渲染更准更好看——嵌套列表层级正确，支持表格 / 任务清单 / 删除线 / 引用，代码块改成带语法高亮和右上角一键复制按钮的卡片，宽表格底部可横向滚动不再溢出。
- **文件预览更顺手**：文件树支持拖拽调整落点、双击直接打开文件，回合结束自动打开新文件的后缀规则统一管理。

### 修复

- **切换会话不再闪动**：在不同会话间切换时，右侧预览区不再整块重新挂载，消除了切换瞬间的闪烁。

## [0.3.2] - 2026-06-23

本版带来内置 AI 出图能力，并对技能面板、会话列表做了体验改进，修复了重连与新建文件夹的两个问题。

### 新功能

- **内置 AI 出图**：可在对话里直接让 Codex / Claude 生成图片——内置一个出图技能，支持文生图，也支持图生图 / 改图（传入参考图做多模态编辑），生成的图自动落到工作区。出图改为流式直连，慢出图不再被中途 60 秒超时砍断。

### 改进

- **技能注入面板搜索框**：技能列表顶部新增搜索框，可按名称或描述快速过滤。
- **会话列表更清爽**：时间标签改为紧凑的英文格式，引擎用带颜色的小药丸区分，状态圆点只在「完成 / 失败」时显示。

### 修复

- **重连后回合内容不再重复**：断线重连、重新进入会话后，正在进行的那一轮对话内容不会再被重复叠加显示。
- **新建文件夹立即可见**：在文件列表里新建文件夹后会立刻出现并自动展开定位，不再「建了却看不见」。

## [0.3.1] - 2026-06-22

本版新增设置里的「软件更新」页，并修复更新提示角标的两个问题。

### 新功能

- **软件更新设置页**：设置新增「软件更新」分区，可查看当前版本并随时手动点「检查更新」（应用本身也每小时自动检查一次）。结果就地显示：已是最新 / 发现新版本（给 GitHub 下载入口）/ 检查失败。开发版不检查更新，按钮会禁用。

### 修复

- **更新角标气泡位置**：发现新版本时左下角的更新提示气泡不再掉出窗口底部被裁切，改为向上弹出、完整显示。
- **点更新角标误开设置页**：点击更新角标只展开提示气泡，不再连带打开设置页面。

## [0.3.0] - 2026-06-22

本版带来项目回收站和重做的 Finder 风格文件列表，并修复了多处会话列表实时同步问题。

### 新功能

- **项目回收站**：删除项目改为「移到回收站」（软删除），不再立即抹掉。设置里新增「回收站」分区，可设置保留天数、随时恢复或彻底删除，过期后自动清理。
- **删除时连带清理**：删除项目、技能或密钥时，会一并清掉它们之间的绑定关系，不再残留无效引用。

### 改进

- **Finder 风格文件列表**：文件列表整体重做——列宽可拖拽调整、右键菜单自选要显示的列、表头吸顶、新增「大小」列、文件夹行双击直接进入、列状态按项目分别记住。
- **画廊视图键盘导航**：用 ←/→ 在画廊里切换文件，聚焦的缩略图会自动滚入可视区；图标尺寸滑块两端图标可点击直接跳到最小/最大。

### 修复

- **会话列表实时同步**：后台新建的会话（如飞书 `/new`）会即时出现在列表里；会话自动起名、置顶和标题变更也会实时同步，不再需要刷新。

## [0.2.1] - 2026-06-21

首个正式记录版本。带来全新的首次启动引导、飞书远程接管、项目分组与图标、可配置的数据目录，以及侧栏和会话视图的整体重做。

### 新功能

- **首次启动引导**：新安装后有分步向导，帮你配好 Codex / Claude 引擎和推荐插件，每步都能「稍后再说」；之后也能在设置里重新打开。
- **飞书远程接管**：把会话桥接到飞书，在飞书里直接查看和操控正在运行的会话。
- **项目分组与图标**：项目可归入自定义分组、拖拽排序，并设置图标（内置图标 / 上传 / 让 AI 生成），侧栏一眼区分。
- **项目级系统提示**：可为每个项目单独设置系统提示词，对 Claude 和 Codex 都生效。
- **推荐工具目录**：在插件页浏览并一键安装推荐的技能 / MCP / 命令行工具，安装前自动做冲突预检。
- **可配置数据目录**：可以把应用数据迁移到你指定的目录，带完整迁移与校验。
- **预览区支持视频和音频**：直接在预览区播放，不再把媒体文件显示成乱码。
- **回合结束自动打开新文件**：每轮对话结束后自动打开本轮新生成、命中你设定后缀的最新文件（可在项目设置关闭）。
- **首页「最近目录」**：快速回到最近用过的文件夹。
- **自动化（Beta）**：新增自动 / 定时任务入口。

### 改进

- **侧栏整体重做**：项目分组、拖拽排序、悬停即可查看并就地编辑项目信息卡、一键收起/展开。
- **顶部改为项目页签**：可拖动重排，圆点实时提示状态（运行中 / 待确认 / 完成未读）。
- **会话与项目自动命名**：根据首句内容起更贴切的名字，并保护你手动改过的名字不被覆盖。
- **会话内即时切换**：Codex 会话内切换权限档位或模型即时生效，无需重启。
- **保持后台运行**：关掉窗口任务仍在后台继续，可在设置开关。
- **更真实的提醒**：提醒改用系统提示音。
- **更干净的全新安装**：不再预填默认密钥与 Provider。
- **设置体验**：显示运行时版本信息，路径类设置改为即时保存。

### 修复

- 修复某些情况下打开后工作区空白。
- 修复 agent 删除项目后侧栏 / 顶部页签不刷新。
- 修复首页模型选择在冷启动时不回显上次选择。
- 修复一批侧栏与页签的布局、视觉问题。

### 已知问题

- mac 安装包未签名，首次打开会被 Gatekeeper 拦截，需**右键 → 打开**绕过。

[0.4.0]: https://github.com/cookaihq/agent-shell/releases/tag/v0.4.0
[0.3.5]: https://github.com/cookaihq/agent-shell/releases/tag/v0.3.5
[0.3.4]: https://github.com/cookaihq/agent-shell/releases/tag/v0.3.4
[0.3.3]: https://github.com/cookaihq/agent-shell/releases/tag/v0.3.3
[0.3.2]: https://github.com/cookaihq/agent-shell/releases/tag/v0.3.2
[0.3.1]: https://github.com/cookaihq/agent-shell/releases/tag/v0.3.1
[0.2.1]: https://github.com/cookaihq/agent-shell/releases/tag/v0.2.1
